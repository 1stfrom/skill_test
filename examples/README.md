# Examples

This directory contains optional examples that demonstrate template style without becoming part of a new project's active analysis plan.

When starting a real project from the template, keep examples only if they are useful teaching references. Active exploratory work should live in `profiling/`.
